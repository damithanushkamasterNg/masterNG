import { MouseActionDirective } from './mouse-action.directive';

describe('MouseActionDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseActionDirective();
    expect(directive).toBeTruthy();
  });
});
